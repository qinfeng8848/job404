<div class="col-md-12">
  <div class="job">
    <%= link_to("投简历", new_job_resume_path(@job), class: "btn btn-default pull-right")%>

    <%= link_to("Edit", edit_job_path(@job), class: "btn btn-primary pull-right")%>
  </div>
  <h2><%= @job.title %></h2>
  <p><%= @job.description %></p>
</div>
